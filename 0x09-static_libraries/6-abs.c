#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @n: the value being checked
 * Return: returns the value of an integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
