#include "main.h"

/**
 * mul - a function to multiply two integers
 * @a: function being multiplied to b
 * @b: function being multiplied to a
 * Return: returns the results
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
